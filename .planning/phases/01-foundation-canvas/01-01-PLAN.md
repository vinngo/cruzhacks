---
phase: 01-foundation-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/layout.tsx
  - app/globals.css
  - postcss.config.mjs
  - tsconfig.json
autonomous: true

must_haves:
  truths:
    - "Next.js application runs locally without errors"
    - "Tailwind CSS utility classes are available"
    - "Serif fonts load for headings"
  artifacts:
    - path: "package.json"
      provides: "Dependencies for Next.js, tldraw, Framer Motion, react-resizable-panels"
      contains: "next, tldraw, framer-motion, react-resizable-panels"
    - path: "app/globals.css"
      provides: "Tailwind imports and serif font configuration"
      min_lines: 15
    - path: "postcss.config.mjs"
      provides: "Tailwind v4 PostCSS configuration"
      contains: "@tailwindcss/postcss"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "app/globals.css"
      to: "Tailwind CSS"
      via: "@import directive"
      pattern: "@import \"tailwindcss\""
---

<objective>
Initialize Next.js project with App Router and install all required dependencies for Phase 1. Configure Tailwind CSS v4 and serif fonts for academic aesthetic.

Purpose: Establish the foundation for the application so subsequent plans can build UI components without setup friction.
Output: Working Next.js development environment with all dependencies installed and configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/vincentngo/dev/cruzhacks/.planning/PROJECT.md
@/Users/vincentngo/dev/cruzhacks/.planning/ROADMAP.md
@/Users/vincentngo/dev/cruzhacks/.planning/STATE.md
@/Users/vincentngo/dev/cruzhacks/.planning/phases/01-foundation-canvas/01-RESEARCH.md
@/Users/vincentngo/dev/cruzhacks/.planning/phases/01-foundation-canvas/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with dependencies</name>
  <files>
    package.json
    app/layout.tsx
    app/page.tsx (temporary, will be replaced in Plan 02)
    tsconfig.json
    next.config.ts
  </files>
  <action>
    Create Next.js 15 project using create-next-app with TypeScript, Tailwind CSS, and App Router enabled.

    Run: `npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --import-alias "@/*"`

    When prompted:
    - Use App Router: Yes
    - TypeScript: Yes
    - Tailwind CSS: Yes
    - src/ directory: No
    - Import alias: @/*

    Then install additional dependencies:
    - tldraw: `npm install tldraw@^4.2.0`
    - framer-motion: `npm install framer-motion@^12.0.0`
    - react-resizable-panels: `npm install react-resizable-panels@^4.4.0`

    IMPORTANT: If create-next-app fails or prompts about non-empty directory, use `--force` flag or initialize in a temporary directory and move files.
  </action>
  <verify>
    Run `npm run dev` and confirm Next.js starts without errors.
    Check package.json contains: next, react, tldraw, framer-motion, react-resizable-panels, tailwindcss.
  </verify>
  <done>
    Next.js development server runs on localhost:3000, package.json has all required dependencies, TypeScript and Tailwind are configured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 and serif fonts</name>
  <files>
    app/globals.css
    postcss.config.mjs
    app/layout.tsx
  </files>
  <action>
    Update globals.css to use Tailwind v4 zero-config approach and add serif fonts for academic feel.

    Replace globals.css content with:
    ```css
    @import "tailwindcss";

    /* Serif font for academic aesthetic */
    @import url('https://fonts.googleapis.com/css2?family=Crimson+Text:wght@400;600&display=swap');

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }

    h1, h2, h3, h4, h5, h6 {
      font-family: 'Crimson Text', serif;
    }
    ```

    Update postcss.config.mjs to:
    ```javascript
    const config = {
      plugins: {
        "@tailwindcss/postcss": {},
      },
    };
    export default config;
    ```

    Update app/layout.tsx metadata to preload fonts:
    - Set title to "Socratic Whiteboard"
    - Set description to "AI-powered math tutoring workspace"

    Ensure globals.css is imported in layout.tsx.
  </action>
  <verify>
    Run `npm run dev` and inspect page in browser DevTools.
    Confirm Tailwind classes work (check computed styles).
    Confirm Crimson Text font loads (Network tab shows font request, Elements tab shows font-family on h1/h2/h3).
  </verify>
  <done>
    Tailwind utility classes apply correctly, serif font loads and displays on headings, no CSS errors in console.
  </done>
</task>

</tasks>

<verification>
Run `npm run dev` and verify:
1. Development server starts without errors
2. Browser shows default Next.js page at localhost:3000
3. DevTools Network tab shows Crimson Text font loading
4. package.json lists all dependencies: next, tldraw, framer-motion, react-resizable-panels
5. No TypeScript compilation errors
</verification>

<success_criteria>
1. `npm run dev` starts successfully
2. All dependencies installed and listed in package.json
3. Tailwind CSS v4 configured with zero-config approach
4. Serif fonts load and apply to headings
5. No build or runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-canvas/01-01-SUMMARY.md`
</output>
